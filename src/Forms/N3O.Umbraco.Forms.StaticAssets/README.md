# N3O.Umbraco.Forms.StaticAssets

Brings in the Umbraco Forms back office and client-side assets alongside `N3O.Umbraco.Forms`.

It contains no code of its own. Install it in the web project, which is where those assets have to
land; a project that only compiles against Forms needs the other package alone.

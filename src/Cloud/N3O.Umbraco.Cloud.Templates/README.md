# N3O.Umbraco.Cloud.Templates

Exposes the subscription's organisation details — its name, registration details, address and logo —
to the template merge engine, so a template can print them without the site storing its own copy.

The values come from what the cloud has published for the subscription, which means a template
follows a change made centrally without the site being redeployed or edited.

# N3O.Umbraco.Workflows.StaticAssets

Brings in the Umbraco Workflow package, which carries the back office interface, alongside
`N3O.Umbraco.Workflows`.

It contains no code of its own. Install it in the web project so the workflow interface is present
as well as the assembly reference the other package provides.

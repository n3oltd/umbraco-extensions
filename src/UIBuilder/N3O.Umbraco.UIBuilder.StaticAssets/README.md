# N3O.Umbraco.UIBuilder.StaticAssets

Brings in Konstrukt's back office assets alongside `N3O.Umbraco.UIBuilder`.

It contains no code of its own. Install it in the web project, which is where those assets have to
land; the code that configures Konstrukt is in the other package.

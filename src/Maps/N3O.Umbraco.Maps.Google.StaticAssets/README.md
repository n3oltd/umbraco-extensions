# N3O.Umbraco.Maps.Google.StaticAssets

Brings in the `Our.Umbraco.GMaps` package, which carries the back office editor itself, alongside
`N3O.Umbraco.Maps.Google`.

It contains no code of its own. Install it in the web project so a site gets the editor as well as
the code that configures it.

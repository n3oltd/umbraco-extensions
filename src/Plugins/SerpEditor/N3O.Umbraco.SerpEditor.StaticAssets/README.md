# N3O.Umbraco.SerpEditor.StaticAssets

Ships the back office editor for the SERP property, which previews how the page will appear in
search results, with a targets file that copies it into the consuming site.

It contains no C#. `N3O.Umbraco.SerpEditor` provides the server side; without this package the
property has no editor.

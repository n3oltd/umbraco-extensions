# N3O.Umbraco.WelcomeDashboard.StaticAssets

Ships the view and language file for the welcome dashboard, with a targets file that copies them
into the consuming site.

It contains no C#. `N3O.Umbraco.WelcomeDashboard` registers the dashboard that uses them. A site
replaces the wording by overriding the language file rather than the view.

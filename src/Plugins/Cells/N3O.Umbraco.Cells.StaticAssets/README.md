# N3O.Umbraco.Cells.StaticAssets

Ships the back office editor for the cells property, an App_Plugins bundle built on Handsontable,
with a targets file that copies it into the consuming site.

It contains no C#. `N3O.Umbraco.Cells` provides the server side; without this package the property
stores and reads its value but has no editor.

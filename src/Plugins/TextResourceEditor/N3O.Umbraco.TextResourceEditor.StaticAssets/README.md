# N3O.Umbraco.TextResourceEditor.StaticAssets

Ships the back office editor for the text resource property, with a targets file that copies it into
the consuming site.

It contains no C#. `N3O.Umbraco.TextResourceEditor` provides the server side; without this package
the property has no editor.

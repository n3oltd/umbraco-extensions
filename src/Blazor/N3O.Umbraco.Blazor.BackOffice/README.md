# N3O.Umbraco.Blazor.BackOffice

Ships the App_Plugins script that lets Blazor components run inside the Umbraco back office, and a
targets file that copies it into the consuming site on build.

It contains no C#. Install it alongside `N3O.Umbraco.Blazor`, which provides the hosting; this
package only carries the client-side asset.

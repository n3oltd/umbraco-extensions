# N3O.Umbraco.Scheduler.StaticAssets

Ships the back office dashboard view that frames the Hangfire dashboard, and its language file, with
a targets file that copies them into the consuming site.

It contains no C#. `N3O.Umbraco.Scheduler` mounts Hangfire itself; this package only puts it where a
back office user will find it.
